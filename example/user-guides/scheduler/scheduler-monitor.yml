apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-scheduler
  labels:
    team: frontend
spec:
  namespaceSelector:
    any: true
    matchNames:
      - kube-system
  selector:
    matchLabels:
      app: kube-scheduler
  endpoints:
    - port: http
